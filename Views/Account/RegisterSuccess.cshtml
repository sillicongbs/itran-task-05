@{
    ViewData["Title"] = "Registration Successful";
    var email = (ViewBag.Email as string) ?? "";
    var token = (ViewBag.Token as Guid?) ?? Guid.Empty;
    var showDirect = (bool)(ViewBag.ShowDirectConfirmLink ?? false);

    // Build an absolute confirm URL using the app base (for display) or a relative for the href:
    string relativeConfirmUrl = $"/Account/ConfirmEmail?email={Uri.EscapeDataString(email)}&token={token}";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
<div class="container d-flex justify-content-center align-items-center" style="min-height: 65vh;">
    <div class="card shadow-lg rounded-4 p-4 border-0" style="max-width: 550px; width:100%;">
        <div class="card-body text-center">
            <div class="mb-3">
                <i class="bi bi-check-circle-fill text-success" style="font-size: 3rem;"></i>
            </div>
            <h4 class="fw-bold mb-2 text-success">Thank You for Registering!</h4>
            <p class="mb-4" style="font-size:1.11rem;">
                <span class="fw-semibold text-success">
                    We've sent a confirmation email to your inbox.
                    @if (!string.IsNullOrWhiteSpace(email))
                    {
                        <strong>@email</strong>
                    }
                </span><br>
                <span class="fw-medium text-dark">
                    Please click the confirmation link in the email to activate your account.
                </span>
            </p>
            <div class="alert alert-info d-inline-block mx-auto mb-4 py-2 px-3" style="font-size:1.05rem;">
                <i class="bi bi-info-circle-fill me-1"></i>
                <span class="fw-semibold">Can’t find the email?</span>
                <span class="text-secondary">Check your Spam or Junk folder.</span>
            </div>
            <div>
                @if (showDirect && !string.IsNullOrWhiteSpace(email) && token != Guid.Empty)
                {
                    <div class="alert alert-warning">
                        <strong>Developer shortcut:</strong> confirm without opening the email.
                    </div>
                    <a class="btn btn-warning" href="@relativeConfirmUrl">Confirm now</a>
                }
                <a asp-action="Login" class="btn btn-primary px-4 fw-semibold">Go to Login</a>
            </div>
        </div>
    </div>
</div>
@* <div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">Check your e-mail</h5>
                </div>
                <div class="card-body">
                    <p class="mb-3">
                        We’ve sent a confirmation link to
                        @if (!string.IsNullOrWhiteSpace(email))
                        {
                            <strong>@email</strong>
                        }
                        else
                        {

                            <strong>your e-mail</strong>
                        }
.
                        Click the link in your inbox to activate your account.
                    </p>

                   
                    @if (showDirect && !string.IsNullOrWhiteSpace(email) && token != Guid.Empty)
                    {
                        <div class="alert alert-warning">
                            <strong>Developer shortcut:</strong> confirm without opening the email.
                        </div>
                        <a class="btn btn-warning" href="@relativeConfirmUrl">Confirm now</a>
                    }

                    <a class="btn btn-link" href="/Account/Login">Go to Login</a>
                </div>
            </div>
        </div>
    </div>
</div> *@
