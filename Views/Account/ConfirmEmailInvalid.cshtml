@{
    ViewData["Title"] = "Invalid Confirmation Link";
}
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6">
            <div class="card border-danger">
                <div class="card-header bg-danger text-white">
                    <h5 class="mb-0">Invalid link</h5>
                </div>
                <div class="card-body">
                    <p class="mb-3">Sorry, this confirmation link is invalid or has already been used.</p>
                    <a class="btn btn-primary" href="/Account/Login">Go to Login</a>
                </div>
            </div>
        </div>
    </div>
</div>
